import math

def determining_factor_calculated(flow, squareroot_rp):
    det_calc = flow / squareroot_rp
    return det_calc

def bounds(value1, value2):
    bound = (value1/value2) * 100
    return bound